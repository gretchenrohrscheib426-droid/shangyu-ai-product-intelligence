# Locally added integration source references

These are V2 modules introduced during the recorded local work, copied verbatim with hashes in [code provenance](../docs/code-provenance.json). No modified upstream files are included. Imports of omitted `app`, engine packages and external SDKs mean this directory is **not independently runnable**. Offline tests exercise only `portfolio_code` pure extracts; CI compiles reference syntax without importing live integrations.

Review the real scheduling, data selection and report logic alongside [code walkthrough](../docs/code-walkthrough.md). Full deployment requires lawfully obtained upstream components and separately approved credentials/data. Original additions retain default copyright without an additional reuse grant; see the root LICENSE and final license audit.
